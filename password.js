// Password Validator

// Create a variable called password and set it’s value to a string of your choice

// Write a conditional statement that checks if the password meets the following constraints:
//At least 10 characters long
//Contains at least one letter and one number

// If the password meets the constraint, console log a success message
// If the password fails the constraint, console log a failure message

var password = "password"


// testing for 10 characters
if(password.length > 10){
    console.log('valid password length')
} else{
    console.log('invalid password length')
}

// testing for one letter and a number
if(password === 1, 2, 3, 4, 5, 6, 7, 8, 9, 0){
    console.log('password contains a number')
} else{
    console.log('password requires a number')
}


/[a-zA-Z]/

// if(password === ){
//     console.log('password contains a letter')
// } else{
//     console.log('password requires a letter')
// }
